{
  "name": "rendercara",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cámara local con detección de caras (MediaPipe) y desenfoque Gaussiano (OpenCV) antes de guardar.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.21"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
